/*
*/

//import libs
const { readFileSync } = require('fs');
const lines = readFileSync('example.txt', 'utf8').trim().split('\n')

// parse the file system information for movement, dir, file and size
function planks(lines) {

  return ;
}

